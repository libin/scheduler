%b Current
= @user.name
%br
%select{ "onchange"=>"window.open(this.options[this.selectedIndex].value,'_top')" }
  %option
    = "- Select User -"
  - for user in @users
    %option{ "value"=>"/#{action_name}/?start_date=#{@start_date}?user_id=#{user.id}"}
      = user.name
      