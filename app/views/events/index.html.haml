#users 
  = render :partial => "select_index_user"
#cal
  %h3
    = link_to "« Previous Month", "/events?date=#{@last_month.strftime('%Y-%m-%d')}"
    = @date.strftime(" %B")
    = @date.year
    = link_to " Next Month »", "/events?date=#{@next_month.strftime('%Y-%m-%d')}"

  - calendar_for(@events, :year => @date.year, :month => @date.month) do |t| 
    = t.head('Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday') 
    - t.day(:day_method => :starts_at, :valign => "top", :id => "hover") do |day, tasks|
      .day
        = day.day
      - tasks.each do |task|
        #event
          #delete
            = link_to 'x', event_path(task.id), :confirm => 'Are you sure?', :method => :delete, :confirm => "Are you sure?", :method => :delete
          = task.starts_at.strftime('%H:%M')
          = link_to task.name, event_path(task.id)


= link_to 'New event', new_event_path 